# Archive

This directory contains historical outputs or legacy artifacts that are useful for traceability
but are not part of the active training, inference, or analysis entrypoints.

Current runs should write to `outputs/`, `outputs_set/`, `outputs_ddpm/`, or `results/`.
Large checkpoints should remain outside version control.
