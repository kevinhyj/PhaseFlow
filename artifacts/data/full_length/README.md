# Full-Length Data

Full-length protein datasets, feature caches, and raw benchmark inputs are not
committed to this repository. Keep large generated data in external storage or
download it from Hugging Face, then point the scripts or configs under
`configs/full_length/` to those local paths when reproducing experiments.

Model checkpoints belong under `artifacts/models/full_length/`, not under
`artifacts/data/`.
