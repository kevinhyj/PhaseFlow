# PhaseFlow Dependencies
# Based on flow_matching and transfusion-pytorch

# === CORE DEPENDENCIES (REQUIRED) ===
torch>=2.0.0
numpy>=1.21.0
pandas>=1.3.0
pyyaml>=6.0
tqdm>=4.64.0

# === TRANSFORMER & ATTENTION (REQUIRED) ===
# Tensor reshaping operations
einops>=0.6.0

# Rotary position embeddings (from transfusion-pytorch)
rotary_embedding_torch>=0.8.4

# === FLOW MATCHING (REQUIRED) ===
# ODE integration for phase generation (from flow_matching)
torchdiffeq

# === OPTIONAL ENHANCEMENTS ===
# Type hints and runtime checking
jaxtyping
beartype
typing-extensions>=4.0.0

# Visualization
matplotlib>=3.5.0
