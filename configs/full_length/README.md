# Full-Length Configurations

- `llps.yaml` configures full-length LLPS training.
- `dpr.yaml` configures full-length DPR training.

Both configurations use paths rooted at `artifacts/` for data and models and
write generated outputs under `runs/`. They describe the released training
interfaces without embedding machine-specific paths.
